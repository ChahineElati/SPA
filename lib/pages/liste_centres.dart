import 'package:flutter/material.dart';

class ListeCentres extends StatelessWidget {
  const ListeCentres({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('liste des centres');
  }
}
